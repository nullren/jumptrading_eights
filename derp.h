#include "Data.h"
#include <stdio.h>
#include <cmath>

float compute(const Data& d);
float kahan(float *, int, int);
float power(const float);
float harmonic(const float *);
float harmonic_mean(float *);

